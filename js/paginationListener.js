
export const paginationListener = (element) => {


  element.addEventListener("click", async event => {
    
    console.log(NAME);
    // const prod = await fetchProduct("", ,  )
    // addContent(prod, categories)
    // pagination(prod.pages, 1)
    // setCount(prod.count)
  });
}